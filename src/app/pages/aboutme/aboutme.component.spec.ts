import { AboutmeComponent } from './aboutme.component';

describe('AboutmeComponent', () => {
  let component: AboutmeComponent;

  beforeEach(() => {
    component = new AboutmeComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
